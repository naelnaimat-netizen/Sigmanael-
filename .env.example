# Example environment configuration for Sigmanael
# Copy this to .env and fill in your values

# User Configuration
USER_ID=my_user_id
USER_NAME=Your Name

# Assistant Configuration
ASSISTANT_NAME=Sigmanael

# Optional: OpenAI API Key for advanced AI features
# OPENAI_API_KEY=sk-...

# Server Configuration (for API mode)
HOST=0.0.0.0
PORT=8000

# Data Storage (defaults to ~/.sigmanael)
# DATA_DIR=/custom/path/to/data
